<!-- app/views/admin/plans/index.html.erb -->
<div class="admin-container">
  <%= render 'admin/sidebar' %>

  <div class="admin-content">
    <!-- Header -->
    <div class="page-header">
      <div class="header-content">
        <h1>📋 Subscription Plans</h1>
        <p class="header-subtitle">Manage pricing tiers and features</p>
      </div>
      <button class="btn-primary add-plan-btn">
        <span class="btn-icon">+</span>
        Add New Plan
      </button>
    </div>

    <!-- Flash Messages -->
    <% if flash[:notice] %>
      <div class="alert alert-success">
        <span class="alert-icon">✓</span>
        <%= flash[:notice] %>
      </div>
    <% end %>
    <% if flash[:alert] %>
      <div class="alert alert-warning">
        <span class="alert-icon">⚠</span>
        <%= flash[:alert] %>
      </div>
    <% end %>

    <!-- Plans Grid -->
    <% if @plans.any? %>
      <div class="plans-grid">
        <% @plans.each do |plan| %>
          <div class="plan-card" data-plan-id="<%= plan.id %>">
            <!-- Plan Header -->
            <div class="plan-card-header">
              <div class="plan-title-section">
                <h3 class="plan-name editable" data-field="name" data-plan-id="<%= plan.id %>">
                  <%= plan.name %>
                </h3>
                <div class="plan-meta">
                  <span class="plan-interval"><%= plan.interval.capitalize %>ly</span>
                  <span class="plan-status">
                    <label class="toggle-switch">
                      <input type="checkbox" class="plan-active-toggle"
                             data-plan-id="<%= plan.id %>"
                             <%= plan.active ? 'checked' : '' %>>
                      <span class="toggle-slider"></span>
                    </label>
                    <span class="status-text"><%= plan.active ? 'Active' : 'Inactive' %></span>
                  </span>
                </div>
              </div>
              <div class="plan-price editable" data-field="price" data-plan-id="<%= plan.id %>">
                <span class="price-currency">€</span>
                <span class="price-value"><%= plan.price %></span>
                <span class="price-period">/<%= plan.interval %></span>
              </div>
            </div>

            <!-- Plan Features -->
            <div class="plan-features editable" data-field="description" data-plan-id="<%= plan.id %>">
              <% plan.description.split(', ').each do |feature| %>
                <div class="feature-item">
                  <span class="feature-icon">✓</span>
                  <%= feature %>
                </div>
              <% end %>
            </div>

            <!-- Plan Actions -->
            <div class="plan-actions">
              <button class="btn-edit edit-plan-btn" data-plan-id="<%= plan.id %>">
                <span class="btn-icon">✎</span>
                Edit Plan
              </button>
            </div>
          </div>
        <% end %>
      </div>

      <!-- Plans Table (Mobile Fallback) -->
      <div class="plans-table-mobile">
        <% @plans.each do |plan| %>
          <div class="plan-row-mobile" data-plan-id="<%= plan.id %>">
            <div class="plan-row-header">
              <div class="plan-row-title">
                <h4 class="editable" data-field="name" data-plan-id="<%= plan.id %>">
                  <%= plan.name %>
                </h4>
                <span class="plan-row-price editable" data-field="price" data-plan-id="<%= plan.id %>">
                  €<%= plan.price %>/<%= plan.interval %>
                </span>
              </div>
              <label class="toggle-switch">
                <input type="checkbox" class="plan-active-toggle"
                       data-plan-id="<%= plan.id %>"
                       <%= plan.active ? 'checked' : '' %>>
                <span class="toggle-slider"></span>
              </label>
            </div>
            <div class="plan-row-features editable" data-field="description" data-plan-id="<%= plan.id %>">
              <%= plan.description %>
            </div>
            <div class="plan-row-actions">
              <button class="btn-edit-mobile edit-plan-btn" data-plan-id="<%= plan.id %>">Edit</button>
            </div>
          </div>
        <% end %>
      </div>

    <% else %>
      <!-- Empty State -->
      <div class="empty-state">
        <div class="empty-icon">📋</div>
        <h3>No Plans Created Yet</h3>
        <p>Create your first subscription plan to get started</p>
        <button class="btn-primary add-plan-btn">
          <span class="btn-icon">+</span>
          Create Your First Plan
        </button>
      </div>
    <% end %>
  </div>
</div>

<!-- Add Plan Modal -->
<div id="add-plan-modal" class="modal">
  <div class="modal-overlay"></div>
  <div class="modal-content">
    <div class="modal-header">
      <h3>Create New Plan</h3>
      <button class="close-modal">&times;</button>
    </div>

    <%= form_with(model: [:admin, Plan.new], id: 'add-plan-form') do |form| %>
      <div class="modal-body">
        <div class="form-group">
          <%= form.label :name, "Plan Name" %>
          <%= form.text_field :name,
              placeholder: "e.g., Premium Monthly",
              required: true,
              class: 'form-control' %>
        </div>

        <div class="form-row">
          <div class="form-group">
            <%= form.label :price, "Price (€)" %>
            <%= form.number_field :price,
                step: 0.01,
                placeholder: "9.99",
                required: true,
                class: 'form-control' %>
          </div>

          <div class="form-group">
            <%= form.label :interval, "Billing Interval" %>
            <%= form.select :interval,
                options_for_select([['Weekly', 'week'], ['Monthly', 'month'], ['Yearly', 'year']], 'month'),
                {},
                required: true,
                class: 'form-control' %>
          </div>
        </div>

        <div class="form-group">
          <%= form.label :description, "Features (comma-separated)" %>
          <%= form.text_area :description,
              rows: 4,
              placeholder: "3 Devices, Unlimited Bandwidth, 24/7 Support",
              required: true,
              class: 'form-control' %>
          <small class="form-hint">Separate each feature with a comma</small>
        </div>

        <div class="form-group">
          <label class="checkbox-label">
            <%= form.check_box :active, checked: true %>
            <span>Make this plan active immediately</span>
          </label>
        </div>
      </div>

      <div class="modal-footer">
        <button type="button" class="btn-secondary close-modal">Cancel</button>
        <%= form.submit 'Create Plan', class: 'btn-primary' %>
      </div>
    <% end %>
  </div>
</div>

<!-- Edit Plan Modal -->
<div id="edit-plan-modal" class="modal">
  <div class="modal-overlay"></div>
  <div class="modal-content">
    <div class="modal-header">
      <h3>Edit Plan</h3>
      <button class="close-modal" data-modal="edit-plan-modal">&times;</button>
    </div>

    <div class="modal-body">
      <input type="hidden" id="edit-plan-id">

      <div class="form-group">
        <label for="edit-plan-name">Plan Name</label>
        <input type="text" id="edit-plan-name" class="form-control" required>
      </div>

      <div class="form-row">
        <div class="form-group">
          <label for="edit-plan-price">Price (€)</label>
          <input type="number" id="edit-plan-price" step="0.01" class="form-control" required>
        </div>

        <div class="form-group">
          <label for="edit-plan-interval">Billing Interval</label>
          <select id="edit-plan-interval" class="form-control" required disabled>
            <option value="week">Weekly</option>
            <option value="month">Monthly</option>
            <option value="year">Yearly</option>
          </select>
          <small class="form-hint">Interval cannot be changed after creation</small>
        </div>
      </div>

      <div class="form-group">
        <label for="edit-plan-description">Features (comma-separated)</label>
        <textarea id="edit-plan-description" rows="4" class="form-control" placeholder="3 Devices, Unlimited Bandwidth, 24/7 Support" required></textarea>
        <small class="form-hint">Separate each feature with a comma</small>
      </div>

      <div class="form-group">
        <label class="checkbox-label">
          <input type="checkbox" id="edit-plan-active">
          <span>Plan is active</span>
        </label>
      </div>
    </div>

    <div class="modal-footer">
      <button type="button" class="btn-secondary close-modal" data-modal="edit-plan-modal">Cancel</button>
      <button type="button" class="btn-primary" id="save-plan-changes">Save Changes</button>
    </div>
  </div>
</div>

<style>
  /* Base Layout */
  .admin-container {
    display: flex;
    min-height: 100vh;
    background-color: #f5f6fa;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
  }

  .admin-content {
    flex: 1;
    padding: 2rem;
    margin-left: 250px;
    max-width: 1400px;
  }

  /* Page Header */
  .page-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2rem;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .header-content h1 {
    color: #2c3e50;
    font-size: 2rem;
    margin: 0 0 0.5rem 0;
  }

  .header-subtitle {
    color: #7f8c8d;
    font-size: 0.95rem;
    margin: 0;
  }

  /* Buttons */
  .btn-primary, .btn-secondary, .btn-edit, .btn-save, .btn-cancel {
    padding: 0.75rem 1.5rem;
    border: none;
    border-radius: 8px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s ease;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.95rem;
  }

  .btn-primary {
    background-color: #9b59b6;
    color: white;
  }

  .btn-primary:hover {
    background-color: #8e44ad;
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(155, 89, 182, 0.3);
  }

  .btn-secondary {
    background-color: #95a5a6;
    color: white;
  }

  .btn-secondary:hover {
    background-color: #7f8c8d;
  }

  .btn-edit {
    background-color: #ff6e08;
    color: white;
    width: 100%;
  }

  .btn-edit:hover {
    background-color: #e67e22;
  }

  .btn-save {
    background-color: #2ecc71;
    color: white;
    width: 100%;
  }

  .btn-save:hover {
    background-color: #27ae60;
  }

  .btn-cancel {
    background-color: #95a5a6;
    color: white;
    width: 100%;
  }

  .btn-cancel:hover {
    background-color: #7f8c8d;
  }

  .btn-icon {
    font-size: 1.1rem;
  }

  /* Alerts */
  .alert {
    padding: 1rem 1.5rem;
    border-radius: 8px;
    margin-bottom: 1.5rem;
    display: flex;
    align-items: center;
    gap: 0.75rem;
    animation: slideIn 0.3s ease;
  }

  .alert-success {
    background-color: #d4edda;
    color: #155724;
    border-left: 4px solid #28a745;
  }

  .alert-warning {
    background-color: #fff3cd;
    color: #856404;
    border-left: 4px solid #ffc107;
  }

  .alert-icon {
    font-size: 1.2rem;
    font-weight: bold;
  }

  @keyframes slideIn {
    from {
      opacity: 0;
      transform: translateY(-10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Plans Grid */
  .plans-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: 1.5rem;
    margin-bottom: 2rem;
  }

  .plan-card {
    background: white;
    border-radius: 12px;
    padding: 1.5rem;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    position: relative;
  }

  .plan-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.12);
  }

  /* Plan Card Header */
  .plan-card-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 1.5rem;
    padding-bottom: 1rem;
    border-bottom: 2px solid #f0f0f0;
  }

  .plan-title-section {
    flex: 1;
  }

  .plan-name {
    font-size: 1.5rem;
    font-weight: 700;
    color: #2c3e50;
    margin: 0 0 0.5rem 0;
    cursor: pointer;
  }

  .plan-name:hover {
    color: #9b59b6;
  }

  .plan-meta {
    display: flex;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .plan-interval {
    font-size: 0.85rem;
    color: #7f8c8d;
    background-color: #ecf0f1;
    padding: 0.25rem 0.75rem;
    border-radius: 20px;
  }

  .plan-status {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .status-text {
    font-size: 0.85rem;
    color: #7f8c8d;
  }

  /* Toggle Switch */
  .toggle-switch {
    position: relative;
    display: inline-block;
    width: 44px;
    height: 24px;
  }

  .toggle-switch input {
    opacity: 0;
    width: 0;
    height: 0;
  }

  .toggle-slider {
    position: absolute;
    cursor: pointer;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: #ccc;
    transition: 0.3s;
    border-radius: 24px;
  }

  .toggle-slider:before {
    position: absolute;
    content: "";
    height: 18px;
    width: 18px;
    left: 3px;
    bottom: 3px;
    background-color: white;
    transition: 0.3s;
    border-radius: 50%;
  }

  input:checked + .toggle-slider {
    background-color: #2ecc71;
  }

  input:checked + .toggle-slider:before {
    transform: translateX(20px);
  }

  /* Plan Price */
  .plan-price {
    text-align: right;
    cursor: pointer;
  }

  .plan-price:hover {
    color: #9b59b6;
  }

  .price-currency {
    font-size: 1.2rem;
    color: #7f8c8d;
  }

  .price-value {
    font-size: 2.5rem;
    font-weight: 700;
    color: #2c3e50;
  }

  .price-period {
    font-size: 1rem;
    color: #95a5a6;
  }

  /* Plan Features */
  .plan-features {
    flex: 1;
    margin-bottom: 1.5rem;
    cursor: pointer;
    padding: 1rem;
    border-radius: 8px;
    transition: background-color 0.2s;
  }

  .plan-features:hover {
    background-color: #f8f9fa;
  }

  .feature-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.5rem 0;
    color: #2c3e50;
    font-size: 0.95rem;
  }

  .feature-icon {
    color: #2ecc71;
    font-weight: bold;
    font-size: 1.1rem;
  }

  /* Editable Fields */
  .editable {
    transition: all 0.2s ease;
  }

  .editable:hover {
    background-color: rgba(155, 89, 182, 0.05);
    border-radius: 4px;
  }

  .edit-input, .edit-textarea {
    width: 100%;
    padding: 0.75rem;
    border: 2px solid #9b59b6;
    border-radius: 8px;
    font-size: 1rem;
    font-family: inherit;
    transition: border-color 0.2s;
  }

  .edit-input:focus, .edit-textarea:focus {
    outline: none;
    border-color: #8e44ad;
    box-shadow: 0 0 0 3px rgba(155, 89, 182, 0.1);
  }

  .edit-textarea {
    min-height: 100px;
    resize: vertical;
  }

  /* Plan Actions */
  .plan-actions {
    display: flex;
    gap: 0.75rem;
    margin-top: auto;
  }

  /* Empty State */
  .empty-state {
    text-align: center;
    padding: 4rem 2rem;
    background: white;
    border-radius: 12px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
  }

  .empty-icon {
    font-size: 4rem;
    margin-bottom: 1rem;
    opacity: 0.3;
  }

  .empty-state h3 {
    color: #2c3e50;
    margin-bottom: 0.5rem;
  }

  .empty-state p {
    color: #7f8c8d;
    margin-bottom: 2rem;
  }

  /* Modal */
  .modal {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1000;
  }

  .modal-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5);
    backdrop-filter: blur(4px);
  }

  .modal-content {
    position: relative;
    background: white;
    margin: 5% auto;
    max-width: 600px;
    border-radius: 12px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
    animation: modalSlideIn 0.3s ease;
    max-height: 90vh;
    overflow-y: auto;
  }

  @keyframes modalSlideIn {
    from {
      opacity: 0;
      transform: translateY(-50px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .modal-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem;
    border-bottom: 1px solid #ecf0f1;
  }

  .modal-header h3 {
    margin: 0;
    color: #2c3e50;
    font-size: 1.5rem;
  }

  .close-modal {
    background: none;
    border: none;
    font-size: 2rem;
    color: #95a5a6;
    cursor: pointer;
    transition: color 0.2s;
    line-height: 1;
    padding: 0;
  }

  .close-modal:hover {
    color: #2c3e50;
  }

  .modal-body {
    padding: 1.5rem;
  }

  .modal-footer {
    padding: 1.5rem;
    border-top: 1px solid #ecf0f1;
    display: flex;
    justify-content: flex-end;
    gap: 1rem;
  }

  /* Form Styles */
  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
  }

  .form-group {
    margin-bottom: 1.5rem;
  }

  .form-group label {
    display: block;
    margin-bottom: 0.5rem;
    font-weight: 600;
    color: #2c3e50;
    font-size: 0.95rem;
  }

  .form-control {
    width: 100%;
    padding: 0.75rem;
    border: 2px solid #ecf0f1;
    border-radius: 8px;
    font-size: 1rem;
    transition: all 0.2s;
    font-family: inherit;
  }

  .form-control:focus {
    outline: none;
    border-color: #9b59b6;
    box-shadow: 0 0 0 3px rgba(155, 89, 182, 0.1);
  }

  .form-hint {
    display: block;
    margin-top: 0.5rem;
    color: #7f8c8d;
    font-size: 0.85rem;
  }

  .checkbox-label {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    cursor: pointer;
    padding: 0.75rem;
    border-radius: 8px;
    transition: background-color 0.2s;
  }

  .checkbox-label:hover {
    background-color: #f8f9fa;
  }

  .checkbox-label input[type="checkbox"] {
    width: 20px;
    height: 20px;
    cursor: pointer;
  }

  /* Mobile Table (Hidden by default) */
  .plans-table-mobile {
    display: none;
  }

  /* Mobile Responsive */
  @media (max-width: 1024px) {
    .admin-content {
      margin-left: 0;
      padding: 1rem;
    }

    .plans-grid {
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: 1rem;
    }
  }

  @media (max-width: 768px) {
    .page-header {
      flex-direction: column;
      align-items: flex-start;
    }

    .header-content h1 {
      font-size: 1.5rem;
    }

    /* Hide grid, show mobile table */
    .plans-grid {
      display: none;
    }

    .plans-table-mobile {
      display: block;
    }

    .plan-row-mobile {
      background: white;
      border-radius: 12px;
      padding: 1rem;
      margin-bottom: 1rem;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    }

    .plan-row-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 1rem;
      padding-bottom: 1rem;
      border-bottom: 1px solid #ecf0f1;
    }

    .plan-row-title h4 {
      margin: 0 0 0.5rem 0;
      color: #2c3e50;
      font-size: 1.2rem;
    }

    .plan-row-price {
      color: #9b59b6;
      font-weight: 600;
      font-size: 1.1rem;
    }

    .plan-row-features {
      margin-bottom: 1rem;
      padding: 0.75rem;
      background-color: #f8f9fa;
      border-radius: 8px;
      font-size: 0.9rem;
      color: #2c3e50;
    }

    .plan-row-actions {
      display: flex;
      gap: 0.5rem;
    }

    .btn-edit-mobile, .btn-save-mobile, .btn-cancel-mobile {
      flex: 1;
      padding: 0.6rem 1rem;
      border: none;
      border-radius: 6px;
      font-weight: 600;
      cursor: pointer;
      font-size: 0.9rem;
    }

    .btn-edit-mobile {
      background-color: #ff6e08;
      color: white;
    }

    .btn-save-mobile {
      background-color: #2ecc71;
      color: white;
    }

    .btn-cancel-mobile {
      background-color: #95a5a6;
      color: white;
    }

    .modal-content {
      margin: 10% 1rem;
      max-width: calc(100% - 2rem);
    }

    .form-row {
      grid-template-columns: 1fr;
    }

    .modal-footer {
      flex-direction: column-reverse;
    }

    .modal-footer button {
      width: 100%;
    }
  }
</style>

<script>
  document.addEventListener('turbo:load', function() {
    initializePlansPage();
  });

  function initializePlansPage() {
    const adminContent = document.querySelector('.admin-content');

    if (!adminContent) {
      console.error('Admin content not found');
      return;
    }

    // Event delegation for all clicks
    adminContent.addEventListener('click', function(e) {
      const target = e.target;

      // Open Add Plan Modal
      if (target.closest('.add-plan-btn')) {
        document.getElementById('add-plan-modal').style.display = 'block';
        document.body.style.overflow = 'hidden';
        return;
      }

      // Close Modals
      if (target.closest('.close-modal')) {
        const modalId = target.closest('.close-modal').getAttribute('data-modal');
        if (modalId) {
          document.getElementById(modalId).style.display = 'none';
        } else {
          // Close all modals
          document.querySelectorAll('.modal').forEach(modal => {
            modal.style.display = 'none';
          });
        }
        document.body.style.overflow = 'auto';
        return;
      }

      // Edit Plan - Open Modal
      if (target.closest('.edit-plan-btn')) {
        e.preventDefault();
        e.stopPropagation();

        const btn = target.closest('.edit-plan-btn');
        const planId = btn.getAttribute('data-plan-id');
        const container = btn.closest('.plan-card') || btn.closest('.plan-row-mobile');

        if (!container) {
          alert('Error: Could not find plan');
          return;
        }

        // Get current plan data
        const nameEl = container.querySelector('.editable[data-field="name"]');
        const priceEl = container.querySelector('.editable[data-field="price"]');
        const descriptionEl = container.querySelector('.editable[data-field="description"]');
        const activeToggle = container.querySelector('.plan-active-toggle');
        const intervalEl = container.querySelector('.plan-interval');

        // Extract values
        const name = nameEl ? nameEl.textContent.trim() : '';
        const priceText = priceEl ? priceEl.textContent.trim() : '';
        const price = priceText.replace(/[€\/\w\s]/g, '').trim();

        // Get description
        let description = '';
        if (descriptionEl) {
          const featureItems = descriptionEl.querySelectorAll('.feature-item');
          if (featureItems.length > 0) {
            description = Array.from(featureItems)
              .map(item => item.textContent.replace('✓', '').trim())
              .join(', ');
          } else {
            description = descriptionEl.textContent.trim();
          }
        }

        const active = activeToggle ? activeToggle.checked : false;
        const interval = intervalEl ? intervalEl.textContent.toLowerCase().replace('ly', '') : 'month';

        // Populate edit modal
        document.getElementById('edit-plan-id').value = planId;
        document.getElementById('edit-plan-name').value = name;
        document.getElementById('edit-plan-price').value = price;
        document.getElementById('edit-plan-description').value = description;
        document.getElementById('edit-plan-active').checked = active;
        document.getElementById('edit-plan-interval').value = interval;

        // Show modal
        document.getElementById('edit-plan-modal').style.display = 'block';
        document.body.style.overflow = 'hidden';
        return;
      }
    });

    // Close modal on overlay click
    document.querySelectorAll('.modal-overlay').forEach(overlay => {
      overlay.addEventListener('click', function() {
        this.closest('.modal').style.display = 'none';
        document.body.style.overflow = 'auto';
      });
    });

    // Save Plan Changes
    const saveBtn = document.getElementById('save-plan-changes');
    if (saveBtn) {
      saveBtn.addEventListener('click', function() {
        const planId = document.getElementById('edit-plan-id').value;
        const name = document.getElementById('edit-plan-name').value;
        const price = parseFloat(document.getElementById('edit-plan-price').value);
        const description = document.getElementById('edit-plan-description').value;
        const active = document.getElementById('edit-plan-active').checked;

        if (!name || !price || !description) {
          alert('Please fill in all fields');
          return;
        }

        // Disable button
        this.disabled = true;
        this.textContent = 'Saving...';

        fetch(`/admin/plans/${planId}`, {
          method: 'PATCH',
          headers: {
            'Content-Type': 'application/json',
            'X-CSRF-Token': document.querySelector('[name="csrf-token"]').content
          },
          body: JSON.stringify({
            plan: {
              name: name,
              price: price,
              description: description,
              active: active
            }
          })
        })
        .then(res => res.json())
        .then(data => {
          if (data.success) {
            location.reload();
          } else {
            alert('Error: ' + (data.error || 'Unknown error'));
            this.disabled = false;
            this.textContent = 'Save Changes';
          }
        })
        .catch(err => {
          console.error(err);
          alert('An error occurred while saving');
          this.disabled = false;
          this.textContent = 'Save Changes';
        });
      });
    }

    // Toggle Active Status (from the card directly)
    document.querySelectorAll('.plan-active-toggle').forEach(toggle => {
      toggle.addEventListener('change', function() {
        const planId = this.getAttribute('data-plan-id');
        const active = this.checked;
        const statusText = this.closest('.plan-status')?.querySelector('.status-text');

        fetch(`/admin/plans/${planId}`, {
          method: 'PATCH',
          headers: {
            'Content-Type': 'application/json',
            'X-CSRF-Token': document.querySelector('[name="csrf-token"]').content
          },
          body: JSON.stringify({ plan: { active } })
        })
        .then(res => res.json())
        .then(data => {
          if (data.success) {
            if (statusText) {
              statusText.textContent = active ? 'Active' : 'Inactive';
            }
          } else {
            alert('Error: ' + (data.error || 'Unknown error'));
            this.checked = !this.checked;
          }
        })
        .catch(err => {
          console.error(err);
          alert('An error occurred');
          this.checked = !this.checked;
        });
      });
    });
  }
</script>
