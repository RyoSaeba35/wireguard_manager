<%# app/views/dashboard/show.html.erb %>
<!-- Status Banner -->
<% if @vpn_server_ips.include?(@user_ip) %>
  <div class="status-banner status-banner-vpn">
    <p class="status-text">🔒 You are protected by VulcainVPN</p>
    <p class="status-detail">IP: <%= @user_ip %> • <%= @country || "Unknown Location" %></p>
  </div>
<% elsif @active_subscription %>
  <div class="status-banner status-banner-warning">
    <p class="status-text">⚠️ VPN Not Connected</p>
    <p class="status-detail">Your IP: <%= @user_ip %> • <%= @country || "Unknown Location" %></p>
  </div>
<% else %>
  <div class="status-banner">
    <p class="status-text">No Active Subscription</p>
    <p class="status-detail">Your IP: <%= @user_ip %> • <%= @country || "Unknown Location" %></p>
  </div>
<% end %>

<div class="dashboard-container">
  <!-- Dashboard Header -->
  <header class="dashboard-header">
    <h1>Welcome, <%= current_user.email.capitalize %></h1>
    <p>Manage your VPN subscription and stay protected</p>
  </header>

  <!-- Subscription Section -->
  <section class="subscription-section">
    <h2>📋 Your Subscription</h2>

    <% if @has_subscription && @active_subscription && @active_subscription.expires_at > Time.now %>
      <!-- Active Subscription -->
      <div class="subscription-alert">
        <div class="alert-icon">
          <svg width="24" height="24" viewBox="0 0 20 20" fill="#ffc33c">
            <path d="M10 0C4.5 0 0 4.5 0 10s4.5 10 10 10 10-4.5 10-10S15.5 0 10 0zM8 15l-5-5 1.4-1.4L8 12.2 14.6 5.6 16 7z" />
          </svg>
        </div>
        <div class="alert-content">
          <h3>✓ Active Subscription</h3>
          <p><strong>Plan:</strong> <%= @active_subscription.plan.name %></p>
          <p><strong>Devices:</strong> Connect up to <%= @active_subscription.max_devices || 3 %> devices</p>
          <p><strong>Expires:</strong> <span id="local-time-text"><%= @active_subscription.expires_at.strftime("%B %d, %Y at %H:%M") %></span></p>
          <p class="countdown">
            <span id="countdown">Calculating...</span>
          </p>
        </div>
      </div>

      <%= link_to "View Subscription Details", user_subscription_path(current_user, @active_subscription), class: "btn btn-block" %>

    <% else %>
      <!-- No Active Subscription -->
      <div class="alert alert-warning">
        <p><strong>No active subscription</strong></p>
        <p>Subscribe now to protect your privacy and secure your connection on up to 3 devices.</p>
      </div>
      <%= link_to "Choose a Plan", new_user_subscription_path(current_user), class: "btn btn-block" %>
    <% end %>

    <!-- Expired Subscriptions -->
    <% if @expired_subscriptions.any? %>
      <div style="text-align: center; margin-top: 1rem;">
        <%= link_to "View Subscription History (#{@expired_subscriptions.count})", expired_subscriptions_path, class: "btn btn-secondary", style: "background-color: #6c757d; max-width: 350px;" %>
      </div>
    <% end %>
  </section>

  <!-- App Download Section -->
  <section class="app-download-section">
    <h2>Download VulcainVPN</h2>

    <!-- Featured download for detected platform (mobile + desktop) -->
    <div id="featured-download" style="display: none;"></div>

    <!-- All platforms text (mobile only - minimalist) -->
    <div class="all-platforms-mobile">
      <p id="other-platforms-text" style="text-align: center; margin: 1.5rem 0 1rem 0; font-size: 0.95rem; color: rgba(255, 195, 60, 0.85);">
        <!-- Will be populated by JavaScript -->
      </p>
      <button id="expand-platforms-btn" onclick="togglePlatforms()" style="display: block; margin: 0 auto; background: rgba(255, 195, 60, 0.2); border: 1px solid rgba(255, 195, 60, 0.4); color: #ffc33c; padding: 0.75rem 2rem; border-radius: 8px; font-weight: 600; cursor: pointer; transition: all 0.3s;">
        Show All Platforms
      </button>
    </div>

    <!-- Platform grid (desktop: always visible, mobile: hidden until expand) -->
    <div id="platform-grid-wrapper">
      <p class="platform-grid-title" style="text-align: center; margin: 1.5rem 0 1rem 0; opacity: 0.85; font-size: 1rem;">
        All available platforms:
      </p>

      <div class="app-download-grid" id="platform-grid">
        <!-- Mobile cards (iOS & Android) -->
        <div class="app-card mobile-card" data-platform="android" data-available="false">
          <div class="app-icon">🤖</div>
          <h3>Android</h3>
          <p>Phones & Tablets</p>
          <span class="status-badge coming-soon">Coming Soon</span>
        </div>

        <div class="app-card mobile-card" data-platform="ios" data-available="false">
          <div class="app-icon">🍎</div>
          <h3>iOS</h3>
          <p>iPhone & iPad</p>
          <span class="status-badge coming-soon">Coming Soon</span>
        </div>

        <!-- Desktop cards (Windows, macOS) -->
        <div class="app-card desktop-card" data-platform="windows" data-available="true">
          <div class="app-icon">🪟</div>
          <h3>Windows</h3>
          <p>Windows 10/11</p>
          <span class="status-badge available">Available Now</span>
          <a href="/downloads/vulcainvpn-windows.exe"
            class="download-btn windows-download" download>Download</a>
        </div>

        <div class="app-card desktop-card" data-platform="macos" data-available="false">
          <div class="app-icon">🍏</div>
          <h3>macOS</h3>
          <p>macOS 11+</p>
          <span class="status-badge coming-soon">Coming Soon</span>
        </div>

        <!-- Linux card -->
        <div class="app-card desktop-card" data-platform="linux" data-available="false">
          <div class="app-icon">🐧</div>
          <h3>Linux</h3>
          <p>Ubuntu, Debian, ...</p>
          <span class="status-badge coming-soon">Coming Soon</span>
        </div>
      </div>
    </div>

    <div class="alert alert-info" style="margin-top: 2rem; background: rgba(255, 195, 60, 0.1); border: 1px solid rgba(255, 195, 60, 0.3); border-radius: 8px; padding: 1rem;">
      <strong>💡 Need Help?</strong> Check out the
      <%= link_to "setup guide", setup_guide_path, style: "color: #ff6e08;" %> or contact support at
      <a href="mailto:support@vulcainvpn.com" style="color: #ff6e08;">support@vulcainvpn.com</a>
    </div>
  </section>

  <!-- Quick Start Guide -->
  <section class="quick-start-section">
    <h2>🚀 Quick Start Guide</h2>

    <div class="quick-start-steps">
      <div class="quick-step">
        <div class="step-number">1</div>
        <div class="step-content">
          <h3>Download the App</h3>
          <p>Install VulcainVPN for iOS, Android, Windows, macOS, or Linux</p>
        </div>
      </div>

      <div class="quick-step">
        <div class="step-number">2</div>
        <div class="step-content">
          <h3>Sign In</h3>
          <p>Open the app and log in with your VulcainVPN account (<%= current_user.email %>)</p>
        </div>
      </div>

      <div class="quick-step">
        <div class="step-number">3</div>
        <div class="step-content">
          <h3>Connect</h3>
          <p>Tap the "Connect" button and you're protected! The app automatically selects the best server for you.</p>
        </div>
      </div>

      <div class="quick-step">
        <div class="step-number">4</div>
        <div class="step-content">
          <h3>Verify Protection</h3>
          <p>Check your IP address at <a href="https://whatismyip.com" target="_blank" style="color: #ff6e08;">whatismyip.com</a> to confirm your connection</p>
        </div>
      </div>
    </div>

    <div class="alert alert-tips" style="margin-top: 1.5rem;">
      <strong>💡 Pro Tip:</strong> You can connect up to <%= @active_subscription&.max_devices || 3 %> devices simultaneously with a single subscription.
    </div>
  </section>

  <!-- Troubleshooting Section -->
  <section class="troubleshooting-section">
    <h2>🔧 Troubleshooting</h2>

    <div class="faq-item">
      <button class="faq-question" onclick="toggleFAQ(this)">
        Can't connect to VPN <span class="faq-icon">+</span>
      </button>
      <div class="faq-answer">
        <p><strong>Try these steps:</strong></p>
        <ul>
          <li>Check your internet connection</li>
          <li>Make sure your subscription is active</li>
          <li>Restart the VulcainVPN app</li>
          <li>Restart your device</li>
          <li>Check if other apps are blocking VPN connections (antivirus, firewall)</li>
          <li>Try toggling airplane mode on/off</li>
        </ul>
      </div>
    </div>

    <div class="faq-item">
      <button class="faq-question" onclick="toggleFAQ(this)">
        Connection keeps dropping <span class="faq-icon">+</span>
      </button>
      <div class="faq-answer">
        <p><strong>Possible solutions:</strong></p>
        <ul>
          <li>Make sure you have a stable internet connection</li>
          <li>Disable battery optimization for VulcainVPN (Android Settings → Apps → VulcainVPN → Battery)</li>
          <li>Check if your network blocks VPN traffic (some public WiFi networks do this)</li>
          <li>Try switching to mobile data to test</li>
          <li>Update the app to the latest version</li>
        </ul>
      </div>
    </div>

    <div class="faq-item">
      <button class="faq-question" onclick="toggleFAQ(this)">
        Slow connection speeds <span class="faq-icon">+</span>
      </button>
      <div class="faq-answer">
        <p><strong>Speed optimization:</strong></p>
        <ul>
          <li>Test your base internet speed without VPN first</li>
          <li>Close background apps that use internet</li>
          <li>Restart your router</li>
          <li>Try connecting to a server closer to your location</li>
          <li>Check if your ISP is throttling VPN traffic</li>
        </ul>
      </div>
    </div>

    <div class="faq-item">
      <button class="faq-question" onclick="toggleFAQ(this)">
        Multiple devices not working <span class="faq-icon">+</span>
      </button>
      <div class="faq-answer">
        <p><strong>Device limit:</strong></p>
        <ul>
          <li>Your plan allows up to <%= @active_subscription&.max_devices || 3 %> simultaneous connections</li>
          <li>Each device gets its own unique configuration automatically</li>
          <li>If you can't connect, check if you've reached your device limit</li>
          <li>Disconnect unused devices to free up slots</li>
        </ul>
      </div>
    </div>

    <div class="faq-item">
      <button class="faq-question" onclick="toggleFAQ(this)">
        Still need help? <span class="faq-icon">+</span>
      </button>
      <div class="faq-answer">
        <p>Contact our support team at <a href="mailto:support@vulcainvpn.com">support@vulcainvpn.com</a></p>
        <p>Include details about:</p>
        <ul>
          <li>Your device type (iPhone 13, Samsung Galaxy S21, etc.)</li>
          <li>App version</li>
          <li>When the issue started</li>
          <li>Error messages you're seeing</li>
        </ul>
      </div>
    </div>
  </section>
</div>

<script>
  // Toggle FAQ answers
  function toggleFAQ(button) {
    const answer = button.nextElementSibling;
    const icon = button.querySelector('.faq-icon');

    if (answer.classList.contains('active')) {
      answer.classList.remove('active');
      icon.textContent = '+';
    } else {
      // Close all other FAQs
      document.querySelectorAll('.faq-answer').forEach(a => a.classList.remove('active'));
      document.querySelectorAll('.faq-icon').forEach(i => i.textContent = '+');

      // Open this one
      answer.classList.add('active');
      icon.textContent = '-';
    }
  }

  // Update countdown
  function updateCountdown(expiresAt) {
    const now = new Date();
    const diff = expiresAt - now;

    if (diff <= 0) {
      document.getElementById("countdown").textContent = "⚠️ Subscription expired";
      return;
    }

    const days = Math.floor(diff / (1000 * 60 * 60 * 24));
    const hours = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));

    let parts = [];
    if (days > 0) parts.push(`${days} day${days !== 1 ? 's' : ''}`);
    if (hours > 0 || days > 0) parts.push(`${hours} hour${hours !== 1 ? 's' : ''}`);
    parts.push(`${minutes} minute${minutes !== 1 ? 's' : ''}`);

    document.getElementById("countdown").textContent = `⏱️ ${parts.join(', ')} remaining`;
  }

  // Initialize countdown
  <% if @active_subscription && @active_subscription.expires_at > Time.now %>
    const expiresAt = new Date("<%= @active_subscription.expires_at.iso8601 %>");
    const localTimeElement = document.getElementById("local-time-text");

    if (localTimeElement) {
      const options = {
        year: 'numeric',
        month: 'long',
        day: 'numeric',
        hour: '2-digit',
        minute: '2-digit'
      };
      localTimeElement.textContent = expiresAt.toLocaleString(undefined, options);
    }

    updateCountdown(expiresAt);
    setInterval(() => updateCountdown(expiresAt), 60000); // Update every minute
  <% end %>

  // Platform Detection and Smart Download
  document.addEventListener('turbo:load', function() {
    detectAndShowPlatform();
  });

  function detectAndShowPlatform() {
    const ua = navigator.userAgent;
    let detectedPlatform = null;
    let platformName = '';
    let downloadUrl = '';
    let isAvailable = true;
    let icon = '';
    let platformSpec = '';

    // Platform data
    const platforms = {
      ios: { icon: '🍎', name: 'iOS', spec: 'iPhone & iPad' },
      android: { icon: '🤖', name: 'Android', spec: 'Phones & Tablets' },
      windows: { icon: '🪟', name: 'Windows', spec: 'Windows 10/11' },
      macos: { icon: '🍏', name: 'macOS', spec: 'macOS 11+' },
      linux: { icon: '🐧', name: 'Linux', spec: 'Ubuntu, Debian, ...' }
    };

    // Specific platform detection
    if (/iPhone|iPad/i.test(ua)) {
      detectedPlatform = 'ios';
      platformName = platforms.ios.name;
      platformSpec = platforms.ios.spec;
      downloadUrl = '#';
      isAvailable = false;
      icon = platforms.ios.icon;
    } else if (/Android/i.test(ua)) {
      detectedPlatform = 'android';
      platformName = platforms.android.name;
      platformSpec = platforms.android.spec;
      downloadUrl = 'https://play.google.com/store/apps/details?id=com.vulcainvpn';
      isAvailable = false;
      icon = platforms.android.icon;
    } else if (/Windows/i.test(ua)) {
      detectedPlatform = 'windows';
      platformName = platforms.windows.name;
      platformSpec = platforms.windows.spec;
      downloadUrl = '/downloads/vulcainvpn-windows.exe';
      isAvailable = true;
      icon = platforms.windows.icon;
    } else if (/Macintosh|Mac OS X/i.test(ua)) {
      detectedPlatform = 'macos';
      platformName = platforms.macos.name;
      platformSpec = platforms.macos.spec;
      downloadUrl = '/downloads/vulcainvpn-macos.dmg';
      isAvailable = false;
      icon = platforms.macos.icon;
    } else if (/Linux/i.test(ua) && !/Android/i.test(ua)) {
      detectedPlatform = 'linux';
      platformName = platforms.linux.name;
      platformSpec = platforms.linux.spec;
      downloadUrl = '/downloads/vulcainvpn-linux.deb';
      isAvailable = false;
      icon = platforms.linux.icon;
    }

    // Build "other platforms" text for mobile
    if (detectedPlatform) {
      const otherPlatforms = Object.keys(platforms)
        .filter(p => p !== detectedPlatform)
        .map(p => `${platforms[p].icon} ${platforms[p].name}`)
        .join(' • ');

      const otherPlatformsEl = document.getElementById('other-platforms-text');
      if (otherPlatformsEl) {
        otherPlatformsEl.innerHTML = `
        <p>Also available:</p>
        <p>${otherPlatforms}</p>
        `;
      }

      // Hide the detected platform card (both mobile and desktop)
      const detectedCard = document.querySelector(`[data-platform="${detectedPlatform}"]`);
      if (detectedCard) {
        detectedCard.classList.add('hidden-platform');
      }
    }

    // Show featured download
    const featuredEl = document.getElementById('featured-download');
    if (featuredEl && detectedPlatform) {
      if (isAvailable) {
        featuredEl.style.display = 'block';
        featuredEl.innerHTML = `
          <h3>${icon} ${platformName}</h3>
          <p>${platformSpec}</p>
          <a href="${downloadUrl}" class="download-btn-large" ${detectedPlatform === 'android' ? 'target="_blank"' : 'download'}>
            Download Now
          </a>
        `;
      } else {
        featuredEl.style.display = 'block';
        featuredEl.innerHTML = `
          <h3>${icon} ${platformName}</h3>
          <p>${platformSpec}</p>
          <p style="font-size: 0.9rem; margin-top: 0.5rem;">Coming soon!</p>
          <a href="mailto:support@vulcainvpn.com?subject=Notify%20me%20when%20${platformName}%20is%20ready&body=Please%20notify%20me%20when%20VulcainVPN%20for%20${platformName}%20is%20available." class="download-btn-large">
            Notify Me When Ready
          </a>
        `;
      }
    }
  }

 // Toggle platform cards on mobile
  function togglePlatforms() {
    const gridWrapper = document.getElementById('platform-grid-wrapper');
    const btn = document.getElementById('expand-platforms-btn');

    if (gridWrapper.classList.contains('expanded')) {
      gridWrapper.classList.remove('expanded');
      btn.textContent = 'Show All Platforms';
    } else {
      gridWrapper.classList.add('expanded');
      btn.textContent = 'Hide Platforms';
    }
  }
</script>

<style>
  /* Animation */
  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(-5px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* Dashboard Container */
  .dashboard-container {
    max-width: 1000px;
    margin: 0 auto;
    padding: 2rem;
  }

  /* Dashboard Header */
  .dashboard-header {
    margin-bottom: 2rem;
    text-align: center;
  }

  .dashboard-header h1 {
    color: #0f0a0e;
    font-size: 2rem;
    margin: 0;
  }

  .dashboard-header p {
    color: #432923;
    font-size: 1rem;
    margin: 0.5rem 0 0 0;
  }

  /* Status Banner */
  .status-banner {
    color: #ffc33c;
    padding: 1rem;
    box-shadow: 0 4px 20px rgba(67, 41, 35, 0.3);
    text-align: center;
  }

  .status-banner-vpn {
    background: #0f0a0e;
    color: #ffc33c;
  }

  .status-banner-warning {
    background: #ff6e08;
    color: #0f0a0e;
  }

  .status-text {
    font-size: 1.1rem;
    font-weight: 600;
    margin: 0;
  }

  .status-detail {
    font-size: 0.9rem;
    opacity: 0.85;
    margin: 0.5rem 0 0 0;
  }

  /* Subscription Section */
  .subscription-section {
    background-color: white;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    margin-bottom: 2rem;
  }

  .subscription-section h2 {
    color: #0f0a0e;
    font-size: 1.5rem;
    margin: 0 0 1.5rem 0;
  }

  /* Subscription Alert */
  .subscription-alert {
    display: flex;
    align-items: flex-start;
    background-color: #fff8e8;
    border-left: 4px solid #ffc33c;
    border-radius: 8px;
    padding: 1.5rem;
    margin-bottom: 1.5rem;
  }

  .subscription-alert-warning {
    background-color: #fff3e0;
    border-left-color: #ff6e08;
  }

  .alert-icon {
    margin-right: 1rem;
    flex-shrink: 0;
  }

  .alert-icon svg {
    width: 24px;
    height: 24px;
  }

  .alert-content h3 {
    margin: 0 0 0.5rem 0;
    font-size: 1.2rem;
    color: #0f0a0e;
    font-weight: 600;
  }

  .alert-content p {
    margin: 0.3rem 0;
    line-height: 1.5;
    color: #432923;
  }

  .alert-content strong {
    color: #0f0a0e;
    font-weight: 600;
  }

  .countdown {
    font-weight: 600;
    color: #ff6e08;
    margin: 0.5rem 0 0;
    font-size: 1rem;
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    display: inline-block;
  }

  .btn-secondary {
    background-color: #6c757d !important;
  }

  .btn-secondary:hover {
    background-color: #5a6268 !important;
  }

  /* App Download Section */
  .app-download-section {
    background: #0f0a0e;
    color: #ffc33c;
    padding: 2rem;
    border-radius: 12px;
    margin-bottom: 2rem;
    box-shadow: 0 4px 20px rgba(15, 10, 14, 0.3);
  }

  .app-download-section h2 {
    color: #ffc33c;
    font-size: 1.5rem;
    margin: 0 0 1rem 0;
    text-align: center;
  }

  .app-download-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
    margin-top: 1.5rem;
  }

  .app-card.hidden-platform {
    display: none !important;
  }

  /* Smart Download Detection Styles */
  #smart-download-message {
    text-align: center;
    padding: 1rem;
    margin-bottom: 1.5rem;
    background: rgba(255, 195, 60, 0.15);
    border-radius: 8px;
    font-size: 1.1rem;
    color: #ffc33c;
    font-weight: 600;
  }

  #featured-download {
    background: rgba(255, 195, 60, 0.1);
    border: 2px solid #ffc33c;
    border-radius: 12px;
    padding: 2rem;
    text-align: center;
    margin-bottom: 1.5rem;
  }

  #featured-download h3 {
    color: #ffc33c;
    margin: 0 0 1rem 0;
    font-size: 1.5rem;
  }

  #featured-download p {
    color: rgba(255, 195, 60, 0.8);
    margin-bottom: 1rem;
  }

  #featured-download .download-btn-large {
    display: inline-block;
    background: linear-gradient(135deg, #ffc33c 0%, #ff6e08 100%);
    color: #0f0a0e;
    padding: 1rem 2.5rem;
    border-radius: 10px;
    text-decoration: none;
    font-weight: 700;
    font-size: 1.2rem;
    transition: all 0.3s ease;
    box-shadow: 0 4px 12px rgba(255, 195, 60, 0.4);
  }

  #featured-download .download-btn-large:hover {
    transform: translateY(-3px);
    box-shadow: 0 6px 20px rgba(255, 195, 60, 0.6);
    color: #0f0a0e;
    text-decoration: none;
  }

  .app-card[data-highlight="true"] {
    border: 2px solid #ffc33c !important;
    background: rgba(255, 195, 60, 0.15) !important;
    transform: scale(1.02);
  }

  @keyframes pulse-glow {
    0%, 100% { box-shadow: 0 4px 12px rgba(255, 195, 60, 0.3); }
    50% { box-shadow: 0 8px 24px rgba(255, 195, 60, 0.6); }
  }

  .app-card[data-highlight="true"] {
    animation: pulse-glow 2s ease-in-out 3;
  }

  .status-badge {
    display: inline-block;
    padding: 0.35rem 0.9rem;
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 600;
    margin-bottom: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  /* .status-badge.available {
    background: rgba(46, 204, 113, 0.2);
    color: #2ecc71;
    border: 1px solid rgba(46, 204, 113, 0.4);
  } */

  .status-badge.available {
    display : none;
  }

  .status-badge.coming-soon {
    background: rgba(255, 195, 60, 0.2);
    color: #ffc33c;
    border: 1px solid rgba(255, 195, 60, 0.4);
  }

  .download-btn.disabled {
    background: #666;
    color: #999;
    cursor: not-allowed;
    opacity: 0.6;
  }

  .download-btn.disabled:hover {
    background: #666;
    color: #999;
    transform: none;
  }

  .app-card {
    background: rgba(255, 195, 60, 0.08);
    border: 1px solid rgba(255, 195, 60, 0.2);
    border-radius: 8px;
    padding: 1.5rem;
    text-align: center;
    transition: transform 0.2s;
  }

  .app-card:hover {
    transform: translateY(-4px);
    background: rgba(255, 195, 60, 0.14);
  }

  .app-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .app-card h3 {
    color: #ffc33c;
    font-size: 1.2rem;
    margin: 0 0 0.5rem 0;
  }

  .app-card p {
    color: rgba(255, 195, 60, 0.8);
    font-size: 0.9rem;
    margin: 0 0 1rem 0;
  }

  .download-btn {
    display: inline-block;
    background: #ffc33c;
    color: #0f0a0e;
    padding: 0.3rem 0.9rem;
    border-radius: 20px;
    text-decoration: none;
    font-weight: 600;
    font-size: 0.75rem;
    transition: all 0.2s;
  }

  .download-btn:hover {
    background: #ff6e08;
    color: #0f0a0e;
    transform: scale(1.05);
  }

  /* Quick Start Guide */
  .quick-start-section {
    background-color: white;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    margin-bottom: 2rem;
  }

  .quick-start-section h2 {
    color: #0f0a0e;
    font-size: 1.5rem;
    margin: 0 0 1.5rem 0;
  }

  .quick-start-steps {
    display: grid;
    gap: 1rem;
  }

  .quick-step {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding: 1rem;
    background: #fdf8f0;
    border-radius: 8px;
    transition: background 0.2s;
  }

  .quick-step:hover {
    background: #fff3d6;
  }

  .step-number {
    background: #ff6e08;
    color: #0f0a0e;
    width: 32px;
    height: 32px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    flex-shrink: 0;
  }

  .step-content h3 {
    margin: 0 0 0.5rem 0;
    color: #0f0a0e;
    font-size: 1.1rem;
  }

  .step-content p {
    margin: 0;
    color: #432923;
    line-height: 1.5;
  }

  /* Buttons */
  .btn {
    display: inline-block;
    background-color: #ff6e08;
    color: #0f0a0e !important;
    padding: 0.75rem 1.5rem;
    border: none;
    border-radius: 8px;
    font-size: 1rem;
    font-weight: 600;
    text-align: center;
    text-decoration: none;
    cursor: pointer;
    transition: all 0.2s;
  }

  .btn:hover {
    background-color: #ffc33c;
    color: #0f0a0e !important;
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(255, 110, 8, 0.3);
  }

  .btn-block {
    display: block;
    width: 100%;
    max-width: 300px;
    margin: 1rem auto;
  }

  /* Alert */
  .alert {
    padding: 1rem;
    border-radius: 8px;
    margin-bottom: 1rem;
  }

  .alert-warning {
    background-color: #fff3d6;
    color: #432923;
    border-left: 4px solid #ffc33c;
  }

  .alert-info {
    background-color: #fff8e8;
    color: #fff8e8;
    border-left: 4px solid #ff6e08;
  }

  .alert-tips {
    background-color: #fff8e8;
    color: #432923;
    border-left: 4px solid #ff6e08;
  }

  /* Troubleshooting Section */
  .troubleshooting-section {
    background-color: white;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    margin-bottom: 2rem;
  }

  .troubleshooting-section h2 {
    color: #0f0a0e;
    font-size: 1.5rem;
    margin: 0 0 1.5rem 0;
  }

  .faq-item {
    margin-bottom: 1rem;
    border-bottom: 1px solid #f0e8d8;
    padding-bottom: 1rem;
  }

  .faq-item:last-child {
    border-bottom: none;
  }

  .faq-question {
    width: 100%;
    text-align: left;
    padding: 1rem;
    background: none;
    border: none;
    cursor: pointer;
    font-size: 1rem;
    color: #0f0a0e;
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-weight: 600;
    transition: background 0.2s;
    border-radius: 4px;
  }

  .faq-question:hover {
    background-color: #fdf8f0;
  }

  .faq-icon {
    font-size: 1.2rem;
    transition: transform 0.2s;
    color: #ff6e08;
  }

  .faq-answer {
    display: none;
    padding: 0 1rem 1rem;
    line-height: 1.6;
    color: #432923;
  }

  .faq-answer.active {
    display: block;
  }

  .faq-answer ul {
    margin: 0.5rem 0 0 1.5rem;
    padding: 0;
  }

  .faq-answer li {
    margin: 0.3rem 0;
  }

  .faq-answer a {
    color: #ff6e08;
    text-decoration: none;
  }

  .faq-answer a:hover {
    text-decoration: underline;
    color: #ffc33c;
  }

  /* Subscription Card */
  .subscription-card {
    background: #fdf8f0;
    border-radius: 8px;
    padding: 1.5rem;
    margin-top: 1rem;
    border-left: 4px solid #ffc33c;
  }

  .subscription-card h3 {
    margin: 0 0 1rem 0;
    color: #0f0a0e;
  }

  .subscription-card p {
    margin: 0.5rem 0;
    color: #432923;
  }

  /* Desktop platforms text - hidden on desktop, shown on mobile */
  .desktop-platforms-mobile {
    display: none;
  }

  /* All platforms mobile section - hidden on desktop */
  .all-platforms-mobile {
    display: none;
  }

  /* Platform grid wrapper */
  #platform-grid-wrapper {
    display: block;
  }

  /* Mobile Responsive */
  @media (max-width: 768px) {
    .dashboard-container {
      padding: 1rem 0;
    }

    .dashboard-header h1 {
      font-size: 1.5rem;
    }

    .alert-info {
      text-align: center;
    }

    .alert-tips {
      text-align: center;
    }

    .status-banner {
      padding: 1rem;
    }

    .subscription-section,
    .app-download-section,
    .quick-start-section,
    .troubleshooting-section {
      padding: 1rem;
    }

    .subscription-alert {
      flex-direction: column;
      text-align: center;
    }

    .alert-icon {
      margin-right: 0;
      margin-bottom: 0.5rem;
    }

    /* Mobile: Hide platform grid initially */
    #platform-grid-wrapper {
      display: none;
    }

    #platform-grid-wrapper.expanded {
      display: block;
    }

    /* Show "other platforms" section on mobile */
    .all-platforms-mobile {
      display: block;
    }

    /* Hide the grid title on mobile */
    .platform-grid-title {
      display: none;
    }

    /* Mobile expanded: 2x2 grid */
    .app-download-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 0.75rem;
      width: 100%;

    }

    /* Make cards more square on mobile */
    .app-card {
      padding: 1rem 0.5rem;
      aspect-ratio: 1 / 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      min-height: 0;
      width: 100%;
    }

    .app-card .app-icon {
      font-size: 2.2rem;
      margin-bottom: 0.4rem;
    }

    .app-card h3 {
      font-size: 1rem;
      margin: 0.2rem 0;
    }

    .app-card p {
      font-size: 0.75rem;
      margin: 0.2rem 0 0.4rem 0;
      line-height: 1.2;
    }

    .app-card .status-badge {
      font-size: 0.65rem;
      padding: 0.2rem 0.5rem;
      margin-bottom: 0;
    }

    /* On mobile when expanded, show ALL cards except detected */
    #platform-grid-wrapper.expanded .app-card {
      display: flex !important;
    }

    /* But still hide the detected platform */
    #platform-grid-wrapper.expanded .app-card.hidden-platform {
      display: none !important;
    }

    /* Expand button hover effect */
    #expand-platforms-btn:hover {
      background: rgba(255, 195, 60, 0.3);
      transform: translateY(-2px);
    }

    .quick-step {
      flex-direction: column;
      text-align: center;
    }

    /* Expand button hover effect */
    #expand-platforms-btn:hover {
      background: rgba(255, 195, 60, 0.3);
      transform: translateY(-2px);
    }

    .quick-step {
      flex-direction: column;
      text-align: center;
    }

    /* Hide desktop cards on mobile */
    .desktop-card {
      display: none !important;
    }

    /* Show desktop platforms text on mobile */
    .desktop-platforms-mobile {
      display: block;
    }

    .quick-step {
      flex-direction: column;
      text-align: center;
    }
  }

  /* Tablet: 2 columns for all cards */
  @media (min-width: 769px) and (max-width: 1024px) {
    .app-download-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>
